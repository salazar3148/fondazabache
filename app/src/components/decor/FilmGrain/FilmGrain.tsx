/**
 * Grano global. Une visualmente madera, tarjetas y texto: es el detalle que
 * separa "web con fondo de madera" de "objeto físico". docs/03 §6.
 * La clase vive en styles/textures.css porque no es exclusiva del componente.
 */
export function FilmGrain() {
  return <div className="filmGrain" aria-hidden="true" />;
}
